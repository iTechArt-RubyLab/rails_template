<% @users.each.with_index(1) do |user, ix| %>
  <%= ix %>
  <%= user.email %>
  <%= user.roles.pluck(:name).join(', ') %>
  <%= link_to 'Show', user_path(user) %>
  <%= link_to 'Edit roles', edit_user_path(user) %>
  <br>
  <br>
<% end %>
